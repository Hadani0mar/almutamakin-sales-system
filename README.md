# نظام المبيعات - تطبيق سطح المكتب

تطبيق سطح مكتب مبني باستخدام Electron و React لإدارة المبيعات والمخزون.

## المميزات

- 📦 **إدارة المنتجات**: إضافة، تعديل، وحذف المنتجات
- 💰 **نظام المبيعات**: معالجة المبيعات مع سلة تسوق
- 📊 **إدارة المخزون**: تتبع المخزون والتنبيهات للمنتجات قليلة المخزون
- 💾 **حفظ تلقائي**: جميع البيانات تُحفظ محلياً في المتصفح

## المتطلبات

- Node.js (الإصدار 14 أو أحدث)
- npm أو yarn

## التثبيت

1. قم بتثبيت التبعيات:
```bash
npm install
```

## التشغيل

### وضع التطوير

لتشغيل التطبيق في وضع التطوير:
```bash
npm run electron-dev
```

هذا الأمر سيقوم بـ:
- تشغيل React على `http://localhost:3000`
- فتح نافذة Electron تلقائياً

### التشغيل المباشر

إذا كان React يعمل بالفعل:
```bash
npm run electron
```

## بناء ملف EXE

لإنشاء ملف قابل للتنفيذ (EXE) لنظام Windows:

```bash
npm run dist
```

سيتم إنشاء ملف التثبيت في مجلد `dist/`.

## البنية

```
electro2/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   ├── ProductManagement.js
│   │   ├── Sales.js
│   │   └── Inventory.js
│   ├── App.js
│   ├── App.css
│   ├── index.js
│   └── index.css
├── electron.js
├── package.json
└── README.md
```

## الاستخدام

1. **إدارة المنتجات**: أضف المنتجات مع السعر والكمية والفئة
2. **المبيعات**: اختر المنتجات وأضفها للسلة ثم أكمل البيع
3. **المخزون**: راقب المخزون وتلقى تنبيهات للمنتجات قليلة المخزون

## ملاحظات

- جميع البيانات تُحفظ محلياً في localStorage
- التطبيق يعمل بدون اتصال بالإنترنت
- يمكن بناء التطبيق كملف EXE لنظام Windows

## الترخيص

هذا مشروع تجريبي للتعلم والاختبار.


